# Cardstack — Product Requirements

Why this exists, who it is for, and what is deliberately out of scope. This is the
product framing; the normative, testable behavior lives per capability under
[`docs/specs/`](./docs/specs/), and the structural map is
[`ARCHITECTURE.md`](./ARCHITECTURE.md). This file links to those rather than
duplicating them.

## Overview

A terminal (TUI) kanban board application, written in Rust: boards made of
columns, columns made of cards, driven entirely from the keyboard.

*(Placeholder — flesh out as the product takes shape: single-board vs
multi-board, local file persistence vs a sync backend, single-user vs
collaborative, etc.)*

## Goals

*(TBD)*

## Non-goals

*(TBD)*

## Users

*(TBD)*
